# Ubuntu Server 24.04 LTS Full Stack Setup

This folder contains scripts to deploy a **complete server stack**:

- Static IP configuration
- Firewall + Fail2Ban
- Docker + Docker-Compose + Portainer
- LAMP stack (Apache + MySQL + PHP)
- Nginx + PHP-FPM
- WordPress Docker deployment
- Samba, FTP/SFTP
- Nextcloud Docker deployment
- Python + AI tools

---

## Deployment Steps

1. Set static IP:
```bash
bash setup_static_ip.sh
